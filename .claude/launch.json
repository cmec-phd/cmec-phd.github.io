{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "jekyll-dev",
      "runtimeExecutable": "bundle",
      "runtimeArgs": ["exec", "jekyll", "serve", "--config", "_config.yml,_config_dev.yml", "--port", "4000"],
      "port": 4000
    }
  ]
}
